Config = {}

-- Timer position options: "bottom-left", "bottom", "bottom-right", "right", "top-right", "top", "top-left", "left"
Config.TimerPosition = "left"

Config.Spheres = {
    {coords = vector3(231.8257, -874.4304, 30.4921), scale = 5.0, alpha = 128, timerDuration = 60},  -- Sphere 1: 60 seconds
    -- Add more spheres as needed: {coords = vector3(x, y, z), scale = radius, alpha = transparency, timerDuration = seconds}
}